<blockquote class="testimonial {% if not wp_is_front_page() %}block{% endif %}">
    {% if not wp_is_front_page() %}
        <img src="{{ background.url }}" alt="">
    {% endif %}
    {{ testimonial_quote | raw }}
    <cite>{{ testimonial_author | raw  }}</cite>
</blockquote>